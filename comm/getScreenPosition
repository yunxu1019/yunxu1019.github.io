03minnerWidth,innerHeight,undefined,innerWidth,innerHeight,undefinedfunction getScreenPosition(target) {
    if (!target) {
        target = {
            offsetTop: 0,
            offsetLeft: 0,
            offsetWidth: +innerWidth,
            offsetHeight: +innerHeight
        };
    }
    ;
    if (!target.getBoundingClientRect) {
        var left = target.offsetLeft, top = target.offsetTop, width = target.offsetWidth, height = target.offsetHeight;
        while (target.offsetParent) {
            target = target.offsetParent;
            left += target.offsetLeft - target.scrollLeft + target.clientLeft;
            top += target.offsetTop - target.scrollTop + target.clientTop;
        }
        var right = left + width, bottom = top + height;
        return {
            left: left,
            right: right,
            bottom: bottom,
            top: top,
            width: width,
            height: height
        };
    }
    var rect = target.getBoundingClientRect();
    var left = rect.left, top = rect.top, width = rect.width, height = rect.height, right = rect.right, bottom = rect.bottom;
    rect = {
        left: left,
        top: top,
        width: width,
        height: height,
        right: right,
        bottom: bottom
    };
    if (rect.width === undefined) {
        rect.width = rect.right - rect.left;
        rect.height = rect.bottom - rect.top;
    }
    return rect;
}
return getScreenPosition;