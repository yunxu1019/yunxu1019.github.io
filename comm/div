06qisNode,document,isString,isArray,appendChild,isElement,cless,isNode,document,isString,isArray,appendChild,isElement,clessfunction div(elem) {
    if (isNode(elem) && /div/i.test(elem.tagName))
        return elem;
    var div = document.createElement('div');
    if (elem) {
        if (isString(elem)) {
            div.innerHTML = elem;
        } else if (isArray(elem)) {
            appendChild(div, elem);
        } else if (isElement(elem)) {
            appendChild(div, [].concat.apply([], elem.children));
        } else if (isNode(elem)) {
            appendChild(div, elem);
        }
    }
    return div;
}
return cless(div, '.-te {\n  box-sizing: border-box;\n  -webkit-tap-highlight-color: rgba(255, 255, 255, 0);\n  -moz-box-sizing: border-box;\n  -webkit-box-sizing: border-box;\n  -o-box-sizing: border-box;\n  -ms-box-sizing: border-box;\n}\n', '-te div div');