042extendIfNeeded,renderDefaults,render,extendIfNeeded,renderDefaults,renderfunction main(page, $scope) {
    if ($scope === void 0) {
        $scope = {};
    }
    extendIfNeeded($scope, renderDefaults);
    render(page, $scope);
    return page;
}
return main;