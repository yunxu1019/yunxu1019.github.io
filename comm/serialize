036encodeURIComponent,undefined,encodeURIComponent,undefinedfunction serialize(map, spliter, equals) {
    if (spliter === void 0) {
        spliter = '&';
    }
    if (equals === void 0) {
        equals = '=';
    }
    var result = [];
    var encode = spliter === '&' && equals === '=' ? encodeURIComponent : function (a) {
        return a;
    };
    for (var k in map) {
        if (map[k] !== undefined)
            result.push(encode(k) + equals + encode(map[k]));
    }
    return result.join(spliter);
}
return serialize;