00qObject,Objectvar getValue = function (o) {
    return o instanceof Object && 'value' in o ? o.value : o;
};
var defaultIsLE = function (sample, search) {
    return getValue(sample) <= getValue(search);
};
var getIndexFromOrderedArray = function (orderArray, searchItem, isLE) {
    if (isLE === void 0) {
        isLE = defaultIsLE;
    }
    var count = 0;
    for (var cx = 0, dx = orderArray.length, ci = cx + dx >>> 1; cx < dx; ci = cx + dx >>> 1) {
        var orderItem = orderArray[ci];
        if (isLE(orderItem, searchItem)) {
            cx = ci + 1;
        } else {
            dx = ci;
        }
    }
    return dx - 1;
};
return getIndexFromOrderedArray;