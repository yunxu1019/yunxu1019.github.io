07qdocument,select,colorpad,render,button,colorlabel,cast,dispatch,cless,document,select,colorpad,render,button,colorlabel,cast,dispatch,clessvar colorpicker, Colorpicker, template, template = Colorpicker = colorpicker = {
        'toString': function () {
            return '<colorlabel ng-change=setModel(this.value)></colorlabel><a ng-click="reset()">\u91cd\u7f6e</a>';
        }
    };
function main(element) {
    if (element === void 0) {
        element = document.createElement('div');
    }
    element.innerHTML = colorpicker;
    var saved_value;
    var selector = element.firstChild;
    select(selector, colorpad(), false);
    render(element, {
        a: button,
        colorlabel: colorlabel,
        setModel: function (value) {
            if (saved_value === void 0) {
                saved_value = element.value || '';
            }
            element.value = value;
            cast(selector, value);
            dispatch(element, 'change');
        },
        reset: function () {
            if (saved_value !== void 0) {
                selector.value = saved_value;
                cast(selector, saved_value);
                dispatch(selector, 'change');
            }
        }
    });
    element.setValue = function (value) {
        element.value = value;
        cast(selector, value);
    };
    return element;
}
return cless(main, '.-wl > .colorlabel,\n.-wl > .button {\n  vertical-align: middle;\n  margin-right: 10px;\n}\n', '-wl colorpicker colorpicker');