0cidocument,navigator,renderPixelRatio,devicePixelRatio,window,screen,requestFullScreen,cancelFullScreen,isFunction,document,navigator,renderPixelRatio,devicePixelRatio,window,screen,requestFullScreen,cancelFullScreen,isFunctionvar hasTarget = function () {
    return !!(document.fullscreenElement || document.msFullscreenElement || document.mozFullScreenElement || document.webkitFullscreenElement);
};
var fullscreen = {
    allow: function () {
        switch (null) {
        case document.fullscreenElement:
        case document.msFullscreenElement:
        case document.mozFullScreenElement:
        case document.webkitFullscreenElement:
            return true;
        }
        return hasTarget();
    }(),
    is: function () {
        if (/chrome/i.test(navigator.userAgent)) {
            var ratio = renderPixelRatio > 1 ? devicePixelRatio : 1;
            var innerHeight = window.innerHeight / ratio;
            var innerWidth = window.innerWidth / ratio;
            return window.innerHeight === screen.height && window.innerWidth === screen.width || screen.height - window.outerHeight <= 16 && screen.width <= innerWidth && window.outerWidth <= innerWidth || screen.width - window.outerWidth <= 16 && screen.height <= innerHeight && window.outerHeight <= innerHeight;
        } else {
            return window.outerHeight === screen.height && window.outerWidth === screen.width;
        }
    },
    hasTarget: hasTarget,
    exec: function (element) {
        requestFullScreen(element);
    },
    exit: function (alert) {
        if (this.hasTarget())
            cancelFullScreen();
        else
            var error = '\u6309 F11 \u9000\u51fa\u5168\u5c4f';
        if (isFunction(alert) && error)
            alert(error);
        return error;
    }
};
fullscreen.request = fullscreen.requestFullscreen = fullscreen.exec;
fullscreen.cancel = fullscreen.exitFullscreen = fullscreen.exit;
return fullscreen;