01eString,Error,String,Errorfunction repeat(string, time) {
    string = String(string);
    if (!string.length)
        return string;
    time = +time | 0;
    if (time <= 0)
        return '';
    if (+time * string.length > 33554431) {
        throw new Error('\u5360\u7528\u5185\u5b58\u8fc7\u5927\uFF01');
    }
    var result = [];
    while (--time >= 0) {
        result.push(string);
    }
    return result.join('');
}
return repeat;